import 'package:flutter/material.dart';

// NOTE : This widget class is responsible for representing a generic menu
// with the purpose of processing a type of file.
// The decompiler and compiler menus / screens are the ones that make use of
// this type of widget.

class FileProcessor extends StatefulWidget {
  const FileProcessor({super.key});

  @override
  State<FileProcessor> createState() => _FileProcessorState();
}

class _FileProcessorState extends State<FileProcessor> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

