import 'package:flutter/material.dart';

class MagickaPupScaffold extends StatelessWidget {
  const MagickaPupScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
